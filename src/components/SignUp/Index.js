import { Grid, Typography } from '@mui/material';
import { SignUpTitle } from '../../styles/SignUp/Index';

function SignUp() {
  return (
    <Grid container spacing={2} justifyContent="center">
        <SignUpTitle variant = "body1">About us</SignUpTitle>
    </Grid>
  )
}

export default SignUp;