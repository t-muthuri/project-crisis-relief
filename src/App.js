import './App.css';
import { Button } from '@mui/material';
import {React, useEffect} from 'react';

function App() {
//use the useEffect hook
  useEffect(() => {
    effect
    return () => {
      cleanup
    };
  }, [input]);
  
  return (
    <div className="App">
      <Button>Hello!</Button>
    </div>
  );
}

export default App;
